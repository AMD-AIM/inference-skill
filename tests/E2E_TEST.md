# E2E Test Redirect

Canonical E2E documentation now lives in:

- `skills/inferencex-optimize/tests/E2E_TEST.md`

This repo-level file is kept as a compatibility entrypoint for older references.

Use either command:

```bash
# Canonical path (preferred)
cat skills/inferencex-optimize/tests/E2E_TEST.md

# Backward-compatible path
cat tests/E2E_TEST.md
```
